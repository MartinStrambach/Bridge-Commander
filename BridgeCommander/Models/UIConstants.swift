import SwiftUI

enum UIConstants {
	/// Button width when abbreviation mode is enabled
	static let abbreviatedButtonWidth: CGFloat = 50

	/// Button width when abbreviation mode is disabled
	static let normalButtonWidth: CGFloat = 75
}
